{{- /*
Inform-only readiness signal: Compliant once the Ingress wildcard certificate reports Ready.
*/ -}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Parameters.certName }}
  namespace: openshift-ingress
status:
  conditions:
    - type: Ready
      status: 'True'
